### Enrutamiento

El proyecto centraliza las rutas en `src/routes/router.tsx`
usando React Router con `<Routes />` y layouts anidados.

El proyecto utiliza React Router con la API declarativa
(`<BrowserRouter>`, `<Routes>`, `<Route>`).
Se prioriza simplicidad sobre Data Router en esta etapa.
